import { Box, Flex, Heading, Tag, Text } from "@chakra-ui/react";
import HeroDetail from "./component/description";

const Hero = () => {
  return (
    <Box>
      <HeroDetail/>
    </Box>
  );
};

export default Hero;
